<UserControl x:Class="MacroHelper.UI.Views.AjudaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.RenderTransform>
        <TranslateTransform x:Name="PageSlide"/>
    </UserControl.RenderTransform>
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard><Storyboard>
                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.22"/>
            </Storyboard></BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{DynamicResource SurfaceBrush}"
                BorderThickness="0,0,0,1" BorderBrush="{DynamicResource BorderLightBrush}" Padding="28,18">
            <StackPanel>
                <TextBlock Text="Central de Ajuda" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBlock FontSize="12" Foreground="{DynamicResource TextMutedBrush}" Margin="0,2,0,0">
                    <Run Text="Perguntas frequentes · SK MacroHelper v"/><Run Text="{Binding VersaoApp}"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <ScrollViewer Padding="28,20,28,28">
            <StackPanel MaxWidth="700">
                <ItemsControl ItemsSource="{Binding Faqs}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1" CornerRadius="10" Padding="18,14" Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="{Binding Pergunta}" FontSize="13" FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock Text="{Binding Resposta}" FontSize="12" TextWrapping="Wrap"
                                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,6,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1" CornerRadius="10" Padding="18,14" Margin="0,8,0,0">
                    <StackPanel>
                        <TextBlock Text="Atalhos úteis" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,8"/>
                        <TextBlock FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,4">
                            <Run Text="Ctrl+Espaço" FontFamily="Consolas" FontWeight="Bold"/><Run Text=" — busca rápida de macros"/>
                        </TextBlock>
                        <TextBlock FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}">
                            <Run Text="Ctrl+Alt+1..9" FontFamily="Consolas" FontWeight="Bold"/><Run Text=" — insere a macro favorita correspondente"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
