<UserControl x:Class="MacroHelper.UI.Views.AuditoriaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.RenderTransform>
        <TranslateTransform x:Name="PageSlide"/>
    </UserControl.RenderTransform>
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard><Storyboard>
                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.22"/>
            </Storyboard></BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{DynamicResource SurfaceBrush}"
                BorderThickness="0,0,0,1" BorderBrush="{DynamicResource BorderLightBrush}" Padding="28,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Auditoria" Style="{StaticResource PageTitleText}"/>
                    <TextBlock Text="Registro de ações de todos os usuários da equipe." Style="{StaticResource PageSubtitleText}"/>
                </StackPanel>
                <TextBox Grid.Column="1" Style="{StaticResource InputStyle}" Height="40" VerticalAlignment="Center"
                         Text="{Binding Busca, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Column="2" Content="Exportar CSV" Style="{StaticResource ButtonSecondary}"
                        Command="{Binding ExportarCsvCommand}" Height="40" Padding="14,0" Margin="10,0,0,0"/>
            </Grid>
        </Border>

        <Border DockPanel.Dock="Top" Margin="28,10,28,0" CornerRadius="10" Padding="14,10"
                Background="{DynamicResource SuccessBgBrush}"
                BorderBrush="{DynamicResource SuccessBrush}" BorderThickness="1"
                Visibility="{Binding Mensagem, Converter={StaticResource NullToVis}}">
            <TextBlock Text="{Binding Mensagem}" FontSize="12" Foreground="{DynamicResource SuccessBrush}"/>
        </Border>

        <ScrollViewer Padding="28,14,28,28">
            <ItemsControl ItemsSource="{Binding Registros}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" Padding="18,13" Margin="0,0,0,6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Acao}" FontSize="13" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <TextBlock Text="{Binding Entidade}" FontSize="11" Margin="8,0,0,0"
                                                   Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Detalhes}" FontSize="11" TextWrapping="Wrap"
                                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,3,0,0"/>
                                    <TextBlock FontSize="10" Foreground="{DynamicResource TextMutedBrush}" Margin="0,4,0,0">
                                        <Run Text="{Binding UsuarioNome}"/><Run Text=" · "/>
                                        <Run Text="{Binding Data, StringFormat='dd/MM/yyyy HH:mm'}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
